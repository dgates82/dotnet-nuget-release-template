name: Release
on:
  push:
    tags:
      - 'v*'
jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '8.0.x'
      # Uncomment if targeting net48 or earlier
      # - name: Setup Mono
      #   run: sudo apt-get install -y mono-complete
      - name: Restore
        run: dotnet restore
      - name: Build
        run: dotnet build --configuration Release --no-restore
      - name: Unit tests
        run: dotnet test --configuration Release --no-build --filter "Category!=Integration"
      # LocalStack block is required for this template's own example (S3NoteStore).
      # Optional once you replace ExampleLibrary with your own project — keep, adapt to a
      # different service, or delete. Seeding (bucket + starting note) happens automatically
      # via docker/seed.sh, mounted as a LocalStack init hook in docker-compose.yml.
      - name: Start LocalStack
        run: docker compose up -d --wait
      - name: Integration tests
        run: dotnet test --configuration Release --no-build --filter "Category=Integration"
      - name: Stop LocalStack
        if: always()
        run: docker compose down -v
  publish:
    needs: build-and-test
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v5
      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '8.0.x'
      - name: Pack
        run: |
          VERSION=${GITHUB_REF_NAME#v}
          dotnet pack --configuration Release /p:Version=$VERSION --output ./artifacts
      - name: NuGet login (OIDC)
        uses: NuGet/login@v1
        id: login
        with:
          user: ${{ secrets.NUGET_USER }}
      - name: Publish to NuGet.org
        run: |
          dotnet nuget push ./artifacts/*.nupkg \
            --api-key ${{ steps.login.outputs.NUGET_API_KEY }} \
            --source https://api.nuget.org/v3/index.json \
            --skip-duplicate
      - name: Upload package artifact
        uses: actions/upload-artifact@v4
        with:
          name: nuget-package
          path: ./artifacts/*.nupkg
